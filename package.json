{
  "name": "pixel-warren",
  "version": "0.1.0-alpha.1",
  "description": "A pixel-art monster-hunting idle game with file-backed save progress.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:artifact": "node scripts/build-artifact.js",
    "test": "node --max-old-space-size=8192 --test --test-force-exit \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mariadb": "^3.5.4"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
